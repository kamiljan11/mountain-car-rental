# Changelog

## [Unreleased]
- **Rebel Travel profile removed** from `src/lib/company.ts`: this repo is public and does not
  keep another company's registration data. Issued documents are unaffected — their rendered
  content is stored — but nothing can be issued as Rebel Travel any more. Resolving a landlord
  now goes through `requireCompany()`, which throws on an unknown key instead of quietly
  falling back to the default: on an invoice that would have swapped the seller unnoticed.
- **Self-hostable**: the operator's identity (brand, kennitala, VSK-nr, address, phone,
  pickup points) moved out of 45 places in 11 files into `src/lib/company.ts`, the only
  file in the application code that names a company. Rendered output is unchanged.
  `src/lib/company.test.ts` covers the two display formatters and fails if those details
  reappear elsewhere under `src/` or in `supabase/seed.sql`. README gains "Running it
  yourself": migrations give an empty database, the single account comes from
  `APP_USER`/`APP_PASSWORD`, there is deliberately no hosted demo. `supabase/seed.sql`
  no longer seeds `rental.settings` with the real company — it carried a stale address a
  migration had already fixed.
- **Docs, not behaviour** — repo made "github-ready" for its public, closed/reference status:
  `README.md` status corrected to closed/reference (was still saying "production, internal";
  the repo visibility claim was also stale), `docs/ARCHITECTURE.md` + `docs/GLOSSARY.md` (new,
  grounded in the current code), two real ADRs (`docs/adr/0001-...`, `docs/adr/0002-...`),
  `LICENSE` (was missing — all rights reserved), `docs/quality/BACKLOG.md` (SQL-lint HIGH
  findings + large-file split plan, all deliberately deferred with a reason).
- **Type safety**: removed the remaining `any`/`eslint-disable` in `src/lib/db.ts` and
  `src/lib/book-public.ts` in favour of row types matching the actual Supabase `select()`
  columns. This surfaced one real latent bug while typechecking: `toVehicle()` assigned
  `year`/`mileage` straight from the row without the `Number(...)` coercion every other numeric
  field already had (`dailyRate` right below it, and every field in `toBooking`/`toInvoice`) —
  harmless while Postgres returns a JS `number`, but a string would have silently flowed into a
  field typed `number`. Fixed to match the existing pattern; every other mapper was already
  correct (checked all of them).
- **Silent catches now log with context** (`console.error("<where>: <what failed>", e)`) across
  the API routes, `auth.ts`, and the client pages/components that previously swallowed the
  error entirely — same user-facing behaviour, but a failure is now visible in logs/console
  instead of disappearing.
- **Test**: `src/lib/invoice.test.ts` (new) — regression coverage for `rentalDays` (the
  16→26 = 10-days billing bug) and `invoiceAmounts` (VAT defaulting to 0%). `rentalDays` is now
  exported from `invoice.ts` so it's testable; no change to either function's logic.
- **CI**: `actions/checkout`/`setup-node` pins bumped to `v4.4.0`, `release.yml`'s checkout pin
  restored (was accidentally left unpinned), `tsc -b` vs `--noEmit` now auto-selected by whether
  `tsconfig.json` uses project references, `secrets-scan` (gitleaks) job reordered next to the
  other quality jobs. `.env.example` corrected to the variable names the code actually reads
  (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`/`EMAIL_FROM` — it previously
  listed `NEXT_PUBLIC_SUPABASE_*` names that `src/lib/supabase-admin.ts` never reads).
- `package.json`: added `vitest` + a `test` script (previously no test runner was wired up at
  all, despite `docs`/CI referring to one). New `vitest.config.ts` excludes `e2e/**` — vitest's
  default file matching was picking up the scaffolded (Playwright) `e2e/smoke.spec.ts` and
  failing the whole run on a missing `@playwright/test`, unrelated to the unit tests.

## 2026-07-31 (41) — Pojazdy „inactive" znikają z kalendarza/pulpitu/kreatora (3 Dustery poza flotą)
- **Status pojazdu `inactive` = auto już nie w naszej flocie**: kalendarz nie pokazuje jego wiersza, pulpit pomija je we wszystkich kaflach, kreator rezerwacji nie oferuje go przy nowym wpisie (przy edycji starego wpisu opcja zostaje, żeby select nie przestawił auta po cichu). Wpisy/historia w bazie i Flota (ze statusem) — bez zmian. Odwracalne: status z powrotem na „active" i auto wraca wszędzie.
- **Dane: 3 Dustery oznaczone `inactive` na prod** (Dacia Duster - górki FZZ82, Dacia Duster Namiot II BPS82, Daci Duster Namiot PFS75) — Kamil: „już tych aut nie mamy". ⚠️ Na tych autach zostały żywe wpisy (rez. 90 000 ISK do 04.08 na BPS82 + 4 blokady z klientami z notatek) — NIE anulowane, decyzja Kamila. — OWU: twarde zabezpieczenie Wynajmującego (research 6 wypożyczalni IS + prawo islandzkie)
- **Nowe OWU w szablonie umowy** (PL+EN, §1–§9) na bazie researchu realnych warunków Blue Car Rental / Lava / Lotus / Icerental 4x4 / Hertz IS / Go Car Rental oraz islandzkich ustaw. **Dotyczy tylko umów generowanych od teraz** — podpisane umowy to zamrożone snapshoty `content` w bazie (zweryfikowane: 2 podpisane + 1 wysłana nietknięte; renderują się wyłącznie z `content`, nigdy z szablonu).
- **Nowe klauzule ochronne**: pełna islandzka lista wykluczeń (podwozie/skrzynia/sprzęgło, szkody wodne i przeprawy rzeczne, **drzwi wyrwane wiatrem**, burze piaskowe/popiołowe/żwir, opony/felgi/szyba, złe paliwo, wnętrze, kolizje ze zwierzętami, ignorowanie kontrolek); off-road = nielegalny (ustawa 60/2013) → mandaty + rekultywacja + **kara umowna 300 000 ISK** + utrata ochron; F-roads/drogi zamknięte min 75 000 ISK; DUI = utrata ochron + regres ubezpieczyciela (ustawa 30/2019) + 150 000 ISK; **kierowca spoza umowy = utrata wszystkich ochron + solidarna odpowiedzialność + 45 000 ISK**; wymogi kierowcy (20 lat / prawko ≥1 rok); procedura szkody: policja przy kolizji/zwierzęciu + **pisemne zgłoszenie do 12 h** pod rygorem pełnej odpowiedzialności; spóźnienie >1 h = doba + 10 000 ISK/dobę; odbiór porzuconego auta 400 ISK/km min 35 000; palenie 50 000; sprzątanie do 30 000; paliwo koszt + 5 000; mandaty **+ 4 500 ISK opłaty administracyjnej/szt.**; klauzula **kílómetragjald (ustawa 100/2025)** — stany licznika w umowie, przeniesienie bez marży; **autoryzacja obciążenia karty do 6 mies. po zwrocie** (z powiadomieniem e-mail); GPS/telematyka; repossession przy istotnym naruszeniu; e-podpis (eIDAS/ustawa 55/2019); **prawo islandzkie + Héraðsdómur Reykjaness** (zamiast polskiego KC — firma jest islandzka); kaucja nie ogranicza odpowiedzialności; brak zwrotu za wcześniejszy zwrot auta.
- **Umowa (tabela)**: nowe wiersze „Kierowcy dodatkowi (imię, nazwisko, nr prawa jazdy)" i „Kontakt alarmowy Wynajmującego (24/7)" — wymagane przez reg. 840/2015 art. 9 do ustawy 65/2015. Nowa sekcja **„Oświadczenie Najemcy"** (PL+EN) przed podpisem — potwierdzenie pouczenia o przepisach, zakazie off-road, wietrze, zwierzętach (obowiązek informacyjny wobec zagranicznych najemców z art. 7 ustawy 65/2015 + dowód na piśmie).
- Kwoty opłat umownych ustawione na poziomach rynkowych (Blue/Lava/Lotus/Icerental) — proporcjonalność pod art. 36 ustawy 7/1936 (klauzule abuzywne); do edycji w `src/lib/contract.ts` (stała `OWU`).
- ⚠️ Notatka compliance (do decyzji, NIE wdrożone): reg. 840/2015 wymaga warunków umowy **minimum po islandzku i angielsku** — nasza umowa jest PL+EN (bez IS). Dodanie wersji islandzkiej wymaga rzetelnego tłumaczenia prawnego, nie auto-translacji.

## 2026-07-21 (39) — Umowy: kontener + status w kalendarzu · Faktury: waluta/metody/termin/PDF
- **Menu „Umowy" (dawniej „Kontrakt")** — pod generatorem doszła **lista wysłanych/podpisanych umów** ze statusem (Podpisana ✓ / Wysłana / Szkic); klik = **podgląd** treści w modalu + „Kopiuj link" do podpisu. Typ `Contract` rozszerzony o `signedAt/signerName/signToken` (mapowane w `db.toContract`).
- **Kalendarz → klik w booking → drawer pokazuje status umowy** (zielono „podpisana ✓ (kto)" / niebiesko „wysłana, czeka na podpis") + przycisk **Podgląd** (otwiera `/sign/[token]` w okienku). Dodany też przycisk **„Wyślij fakturę"** — otwiera `/invoices` z podpiętym klientem/rezerwacją w okienku (ta sama logika co menu Faktury).
- **Faktura — metody płatności**: Revolut / przelew konto ISK (`[konto usuniete z historii]`) / **przelew konto PL** ([wlasciciel konta usuniety], IBAN `[IBAN usuniety z historii]`, UniCredit NV/SA Oddział w Polsce, Dobra 40, 00-344 Warszawa) / gotówka. Blok „Płatność" pokazuje dane właściwe dla wybranej metody.
- **Termin płatności** wybierany: „Przed wydaniem pojazdu" (data startu) albo „Przy zwrocie pojazdu" (data końca).
- **Przelicznik waluty LIVE** (informacyjnie, faktura zostaje w ISK — VSK w ISK): wybór EUR/PLN/USD/GBP → na fakturze „Równowartość ≈ X CUR (kurs z dnia)". Kursy z `open.er-api.com` (baza ISK) przez własny `/api/fx` (serwer bije zewnętrznie, klient tylko w self — bez luzowania CSP). Kurs zapisywany na fakturze (`fx_rate`) w chwili wystawienia.
- **NIP firmy na fakturze**: przy nabywcy-firmie blok pokazuje „Kennitala/NIP" + adres firmy (zweryfikowane).
- **Faktura jako PDF w załączniku** e-maila (nie tylko treść) — generowana serwerowo (`pdf-lib` + font DejaVu Sans dla PL/IS znaków), best-effort (gdy PDF padnie, mail i tak idzie).
- Nowe/zmienione: `src/lib/fx.ts`, `src/app/api/fx/route.ts`, `src/lib/invoice-pdf.ts`, `public/fonts/DejaVuSans.ttf`, migracja `20260721180000_invoice_payment_fields.sql` (kolumny `payment_method/payment_term/display_currency/fx_rate`), rozbudowa `invoice.ts`/`email.ts`/`db.ts`/`invoices/page.tsx`/`contracts/page.tsx`/`Timeline.tsx`/`nav.ts`. Deps: `pdf-lib`, `@pdf-lib/fontkit`.


## 2026-07-21 (38) — Faktury: wystaw + wyślij na e-mail klienta z realnych danych kalendarza
- **Nowa pozycja w menu „Faktury" (`/invoices`).** Wybierasz Sprzedawcę (Mountain Car / Rebel Travel), klienta i rezerwację — faktura **wypełnia się realnymi danymi z bazy** (nabywca: osoba/firma + adres + kennitala/NIP; pozycja: pojazd, okres, liczba dni, stawka; kwoty: netto → VAT (VSK) → brutto; blok płatności: numer konta `[konto usuniete z historii]` + tytuł przelewu). Podgląd na żywo, przyciski **„Wystaw i wyślij fakturę"** oraz **„Drukuj / PDF"**.
- **Numer sekwencyjny z bazy, bezluki w roku, format `RRRR/NNNN`** (np. `2026/0001`) — nadawany **atomowo na serwerze** (`UNIQUE(number)` + retry na 23505), tak jak numer umowy. Podpowiedź w podglądzie to tylko UI; ostateczny numer daje serwer, więc dwie karty/dwóch adminów nie wybiją tego samego numeru księgowego.
- **Kwoty i liczba dni spójne z umową**: dni liczone **po datach** (`differenceInCalendarDays`, dzień zwrotu niewliczony), rozbicie netto/VAT liczone z brutto tylko gdy stawka VAT > 0 (przy 0%/starych wpisach nie deklarujemy fałszywego rozbicia). Zweryfikowane live: [klient 031], Renault Master II RHR89, 26.08–04.09.2026 → **9 dób × 25 000 = 225 000 ISK**, VAT 0%, brutto 225 000; nabywca z realnym adresem/e-mailem; konsola bez błędów.
- **Mail faktury** (`emailInvoice`) inline-styled (klienty pocztowe wycinają `<style>`), z rozbiciem kwot i danymi do przelewu; ta sama serwerowa ścieżka Resend co reszta maili. Snapshot HTML faktury zapisywany na wierszu (dowód treści). Bezpieczeństwo: wszystkie dane klienta **escapowane** przed `dangerouslySetInnerHTML` (ten sam wzorzec co umowa).
- Nowe: migracja `20260721160000_invoices.sql` (tabela `rental.invoices`, RLS `authenticated` full / `anon` revoked), `src/lib/invoice.ts` (`buildInvoice`, `makeInvoiceNumber`, `invoiceAmounts`, `COMPANY_BANK`), `fetchInvoices`/`insertInvoice`/`sendInvoiceEmail` (db+actions), `emailInvoice`, `/invoices`, pozycja w `nav.ts`, mirror w `seed.sql`.
- ⚠️ **Migracja `rental.invoices` NIE jest jeszcze na prod** (klasyfikator zablokował zapis do produkcyjnej bazy). Do wykonania przed użyciem funkcji na produkcji — patrz notatka na końcu sesji.
- ⚠️ Adres firmy: kod (umowa+faktura) używa „Njarðarbraut 6i", vault `COMPANY_MAS_DETAILS` podaje „Njarðabraut 3i". Do potwierdzenia który jest prawidłowy — nie zmieniałem, żeby nie rozjechać z umową.

## 2026-07-20 (37) — 3 poprawki UX: menu zawsze widoczne, kalendarz skacze do wyniku, mail przy WhatsApp
- **Sidebar (desktop): „Zainstaluj aplikację" i „Wyloguj" widoczne na KAŻDEJ stronie.** Wcześniej `<aside>` rósł z treścią, więc na wysokich stronach (kalendarz) stopka schodziła pod widok i trzeba było scrollować całą stronę. Teraz pasek jest `sticky h-screen` — trzyma się wysokości okna, nav scrolluje wewnętrznie, stopka zostaje na dole. Zweryfikowane live: po przewinięciu strony (scrollY=300) „Wyloguj" nadal w widoku.
- **Kalendarz: wpisanie klienta przenosi widok DO niego + podświetla.** Dotąd po wyszukaniu pasek trafienia bywał poza ekranem i trzeba było ręcznie skrolować w bok. Teraz kalendarz **przewija poziomo do pierwszego trafienia**, a pasujące paski dostają **ring + cień** (przy aktywnym wyszukiwaniu widoczne są tylko trafienia). ⚠️ Świadomie: przewijamy przez bezpośrednie `scrollLeft`, nie `scrollTo({behavior:"smooth"})` — smooth bywa ignorowane w części przeglądarek/na kontenerze z overscroll-contain (potwierdzone testem: smooth 800→800, bezpośrednie działa). Zweryfikowane live: [klient 031]/[klient 742]/[klient 631] — scroll skacze do paska, pasek w widoku.
- **Płatność: przy „Wyślij przez WhatsApp" jest teraz opcjonalny „Wyślij mailem".** `RevolutPay` dostał opcjonalny przycisk maila; na Checkliście wydania pojawia się, gdy klient ma e-mail i podpiętą rezerwację (inaczej brak przycisku). Ta sama serwerowa ścieżka `sendPaymentEmail` co w kalendarzu. Zweryfikowane live: przy kliencie z e-mailem widać trójkę Kopiuj link · WhatsApp · Wyślij mailem.

## 2026-07-20 (36) — E-podpis umowy online + diagnostyka maila po wniosku
- **Podpis elektroniczny umowy** (wzorzec `ContractGate` z Reykjawwwik/maskalkulator, doręczenie tokenowym linkiem jak `booking_links`). „Wyślij do klienta" na stronie Kontrakt zapisuje umowę **i od razu wysyła klientowi maila z linkiem do podpisu online** (`/sign/[token]`, ważny 14 dni). Klient widzi pełną treść umowy (ten sam bezpieczny render co w panelu — dane escapowane), wpisuje imię i nazwisko + zaznacza „akceptuję warunki wraz z OWU" → **„Podpisuję umowę"**. Zapisujemy `signer_name`, `signed_at`, oraz `signer_meta` (IP + user-agent) jako dowód. Podpis jest **idempotentny** (druga próba: „umowa już podpisana"), niepodpisany link wygasa po 14 dniach, podpisany zostaje aktywny (klient ma swój egzemplarz). Nowe: migracja `20260720150000_contract_esign.sql` (kolumny `sign_token/sign_expires_at/signed_at/signer_name/signer_meta`, wdrożona na prod), `sign-public.ts` (publiczna warstwa BEZ `requireSession`, scoped po tokenie), `/api/sign/[token]`, strona `/sign/[token]` (standalone), `emailContractSign`, `sendContractForSignature` (db+actions). `/sign/` i `/api/sign/` dodane do publicznych ścieżek (proxy + AppShell). Zweryfikowane na buildzie produkcyjnym: strona 200 publicznie, POST podpisu → zapis w bazie z metadanymi, ponowny POST odrzucony, GET po podpisie = status `signed`. Umowa testowa posprzątana.
- **Mail po zatwierdzeniu wniosku — diagnostyka.** Klucz Resend i domena (`mountaincar.is`) są **verified i działają** (test wysyłki OK), więc gdy mail „nie wychodzi", przyczyna jest w **konfiguracji env, nie w kodzie**. `sendEmail` zyskał wariant `sendEmailResult` zwracający **konkretny powód** porażki; przy zatwierdzaniu wniosku toast pokazuje teraz np. „mail NIE wyszedł: **brak konfiguracji RESEND_API_KEY (env Vercela)**" albo „**Resend HTTP 4xx: …**" albo „wniosek nie ma adresu e-mail klienta" — zamiast samego „nie wyszedł". Diagnoza zamiast zgadywania. Dodatkowo: wszystkie maile wychodzą teraz **multipart (html + text)** — czysta wersja tekstowa poprawia dostarczalność (mniej spamu).
- ⚠️ Do sprawdzenia przez Kamila: jeśli mail nie wychodzi na **Preview** deployach (z PR), to najpewniej `RESEND_API_KEY` jest ustawiony tylko w **Production** env Vercela — dodaj go też do Preview. (Nie mam `VERCEL_TOKEN`, więc env Vercela nie ruszam.)

## 2026-07-16 (35) — Pakiet terenowy: skeleton, Wake Lock, pull-to-refresh, puste stany
Przejście przez skille `design-styles` + `web-bajery`. **Wniosek: stylu NIE zmieniamy** — apka siedzi w „Vercel/shadcn look" (Geist + zinc + 1px bordery + niebieski akcent), czyli dokładnie w rodzinie, którą te skille rekomendują dla data-heavy narzędzi B2B (kuzyn: IBM Carbon). Luka była w **stanach i ergonomii terenowej**, nie w estetyce — i to naprawiono:
- **Skeleton zamiast spinnera** przy pierwszym wczytaniu (`AppSkeleton`): sylwetka nawigacji + kafli + list zamiast „Wczytywanie danych…". Postrzegane jako szybsze, bo pokazuje układ, który się zaraz wypełni. Klasa `.sk` (shimmer w globals.css) respektuje `prefers-reduced-motion` (globalna reguła ścina animację). Zweryfikowane: **44 elementy skeletonu** złapane w oknie pierwszego fetcha (sonda iframe).
- **Wake Lock na Checkliście wydania** (`useWakeLock`): ekran telefonu nie gaśnie przy odhaczaniu punktów na dworze. Degraduje się cicho, gdy przeglądarka nie wspiera/odmówi (to wygoda, nie funkcja krytyczna); odnawia blokadę po powrocie z tła. ⚠️ Realne „ekran nie gaśnie" da się potwierdzić tylko na fizycznym telefonie — w podglądzie zweryfikowane, że API jest dostępne, hook wpięty i strona działa bez błędów.
- **Pull-to-refresh na mobile** (`PullToRefresh`): pociągnięcie w dół u góry ekranu dociąga świeże dane (`refresh()`) **bez przeładowania strony** — lekarstwo na nieaktualny stan w PWA otwartej godzinami (dziś przy liczniku właśnie to ugryzło). Natywny PTR przeglądarki wyłączony (`overscroll-behavior-y: contain`), żeby się nie dublował. Guardy: nie łapie gestu, gdy otwarty jest modal (scroll-lock) ani gdy strona nie jest na samej górze. Zweryfikowane wszystkie 4 stany (poniżej progu → „Pociągnij w dół", powyżej → „Puść, aby odświeżyć", puszczenie → „Odświeżam…", koniec → znika) + guard desktopowy.
- **Puste stany** (`EmptyState`, spójny wygląd: ikona + co to znaczy + akcja): dashboard „Brak wydań i zwrotów" z wyjaśnieniem, lista Rezerwacje z podpowiedzią i działającym **„Wyczyść wyszukiwanie"** (zweryfikowane: 0 → 83 wiersze po kliknięciu).
- Przy okazji: poprawione 2 wcześniejsze błędy lintera (niezescapowane cudzysłowy na Checkliście). Build webpack czysty, bez migracji.

## 2026-07-16 (34) — Tablica rejestracyjna widoczna WSZĘDZIE przy wydaniu + VIN z umowy usunięty
- **Przyczyna „złych numerów rejestracyjnych" przy wydaniu:** apka pokazywała przy wydaniu tylko **nazwę** auta („Renault Master"), a są **dwa** prawie identyczne — **Renault Master (UNZ27)** i **Renault Master II (RHR89)** — więc łatwo pomylić egzemplarz/kluczyki. Umowa robiona przez „Wygeneruj umowę" ze szuflady renderowała rejestrację **poprawnie** (auto z rezerwacji) — to nie był błąd renderu, tylko brak widocznej tablicy w miejscu decyzji. Teraz **tablica jest przy nazwie auta wszędzie**: szuflada kalendarza (wiersz „Pojazd" → „Renault Master · UNZ27"), dashboard „Wydania i zwroty" (obie linie), kreator (nagłówek kroku Oferta), oraz umowa (dropdown wyboru rezerwacji + podsumowanie potwierdzenia). Zweryfikowane live.
- **VIN usunięty z umowy** (na prośbę Kamila; u nas VIN i tak zawsze pusty → „VIN: ——"). Znika z: głównej umowy (linia „Samochód"), protokołu wydania i protokołu zwrotu. W protokołach wiersz „Pojazd" pokazuje teraz **nazwę + tablicę** (pogrubioną). Zweryfikowane: umowa pokazuje „Samochód: Renault Master · Nr rejestracji: **UNZ27**", zero pola VIN.
- ⚠️ Do potwierdzenia przez Kamila: booking [klient 331] (16–26.07) jest przypisany do **Renault Master UNZ27**. Jeśli fizycznie wydałeś **Renault Master II (RHR89)**, trzeba przepiąć auto na rezerwacji — daj znać którym autem realnie jeździ, poprawię. Build webpack czysty, bez migracji.

## 2026-07-16 (33) — Szybkie wpisanie stanu licznika PRZY WYDANIU (2 wejścia)
- Do tej pory stan początkowy licznika dało się wpisać tylko przez pełny kreator (Edytuj → krok Oferta) albo przez okno „Rozlicz km i zakończ" (a to jest zamknięcie najmu, autofocus na stanie PO). Dodane **dedykowane, szybkie wejście** `OdometerStartModal` (jedno pole, zapisuje sam `odometer_start`, nie rusza statusu). Wpięte w **dwa naturalne miejsca**: (1) **szuflada kalendarza** — nowy przycisk „**Wpisz licznik przy wydaniu**" (dla rezerwacji), a jak już spisany pokazuje „Licznik wydania: X km"; dodatkowo w szczegółach doszły wiersze **„Licznik wydania"** i **„Licznik zwrotu"**; (2) **dashboard** — klik w pozycję **wydania** (↑) rezerwacji w „Wydania i zwroty" otwiera od razu to okno (klik w zwrot dalej otwiera rozliczenie km; blok/serwis → edycja). Trzecie miejsce (krok Oferta w kreatorze) zostaje bez zmian. Walidacja: zapis tylko przy poprawnej liczbie km (spójnie z resztą apki). Zweryfikowane live (Playwright): przycisk+wiersz w szufladzie, modal+walidacja (puste/zła wartość = Zapisz nieaktywny), klik wydania na dashboardzie otwiera okno licznika (nie kreator). Build webpack czysty. Bez migracji (kolumny `odometer_start/end` już są).

## 2026-07-16 (32) — Kreator: domyślna stawka VAT = 0% (nie 24%)
- W kroku „Oferta" domyślnie zaznaczone jest teraz **0% zw. / eksport** — zespół świadomie przestawia na **24% standard**, gdy dana rezerwacja tego wymaga (nie odwrotnie). Dotyczy nowych wpisów oraz zamiany blokady/serwisu na rezerwację. Edycja istniejącej rezerwacji nadal pokazuje jej zapisaną stawkę (bez zmian). Zweryfikowane live: nowa rezerwacja → krok Oferta pokazuje „0% zw. / eksport" jako aktywny przycisk. Build webpack czysty.

## 2026-07-16 (31) — Fix: liczba dni = różnica dat (16→26 = 10), nie +1
- **Fix przeliczania dni w cenie/umowie.** Zasada (od Kamila): **liczymy po datach** — 16→26 to **10 dni**, 10→20 to 10 dni, bez znaczenia czy liczyć dniami czy nocami; dzień zwrotu NIE jest kolejną dobą. Wcześniej było `różnica dat + 1` (16.07→26.07 = 11). Ujednolicone we **wszystkich** miejscach liczących dni z zakresu dat: kreator oferty (kwota netto), lista Rezerwacje (kolumna „Dni"), umowa (liczba dni w tabeli), oraz kwota w mailach potwierdzenia/płatności i w kalkulacji ceny linków samoobsługowych (`book-public`). Godziny wydania/odbioru nadal bez wpływu na liczbę dni. Zweryfikowane live: „[klient 305] [klient 808]" (Renault Master, 16.07–26.07) → kreator pokazuje teraz **„na 10 dni"**, 28 000 × 10 = 280 000 ISK (wcześniej 11 × 28 000 = 308 000). Build webpack czysty.
- **Dane (backfill prod):** poprawiono **9 rezerwacji**, które miały w bazie zapisaną kwotę policzoną po staremu (`stawka × (dni+1)`) — teraz `stawka × dni`. Skan całej bazy (39 rez. z zapisaną kwotą): 9 z błędem → poprawione ([klient 808] 308→280k, [klient 067] 75→60k, [klient 859] 104→91k, [klient 241] 36→27k, [klient 375] 175→150k, Odink 416→384k, [klient 676] 330→300k, [klient 631] 160→140k, [klient 031] 250→225k; łączne zawyżenie 197 000 ISK). Zapis **guarded** (każdy wiersz zmieniony tylko gdy nadal miał starą kwotę). **NIE ruszano** 4 rezerwacji z ceną własną/RentHelp/rabatem (nie pasowały do wzoru: [klient 160], [klient 742], [klient 631] 08.08, [klient 887]) ani 26 już policzonych poprawnie. Ponowny skan po zmianie: 0 rez. z błędem. Zweryfikowane live: szuflada Kanady = 280 000 ISK.

## 2026-07-15 (30) — Checklista wyposażenia PER AUTO (Master; Caddy/inne = TODO)
- Sekcja wyposażenia na Checklistcie dobiera się teraz **do auta klienta**: dla **Renault Master** (2 szt., duży żółty kamper) pokazuje jego komplet (18 poz.); dla innych aut — nic, dopóki nie zdefiniujemy ich list. Struktura `EQUIPMENT_SETS` w `lib/checklist.ts` (`match` po nazwie pojazdu + `items`) — dodanie listy dla **VW Caddy** / innych aut = jeden wpis (są TODO-placeholdery w kodzie). Szybka checklista poglądowa (bez wybranego klienta) pokazuje domyślnie Master. Zweryfikowane: klient z Masterem → wyposażenie się pokazuje. Build webpack czysty.

## 2026-07-15 (29) — Zamiana blokady na rezerwację działa z dashboardu/listy
- Przy zamianie **blokady/serwisu na rezerwację** kreator podpowiada teraz **nazwę klienta z notatki** (jak przycisk „Przypisz klienta" z kalendarza) — więc konwersja działa też z **dashboardu i listy Rezerwacje**, nie tylko z dedykowanego przycisku w kalendarzu. Wcześniej z tych miejsc edycja była „goła": po zmianie typu na Rezerwacja utykało się na kroku Klient (brak wskazanego klienta), więc nic się nie zapisywało i wpis zostawał blokadą. Teraz w kroku Klient nazwa jest już wpisana — wystarczy „Nowy klient" (dane podstawione) albo wybrać istniejącego. Wspólny helper `suggestName`/`suggestAmount` (`lib/blockHints.ts`) używany w kalendarzu i kreatorze (koniec duplikacji). Build webpack czysty.

## 2026-07-15 (28) — Dashboard: oddane auto wraca na „Wolne teraz" (fix logiki)
- Fix: auto, którego dzisiejsza rezerwacja jest **zakończona** (zwrócone), było błędnie liczone jako „zajęte dziś" — filtr patrzył tylko na daty (`start ≤ dziś ≤ end`), nie na status — więc nie pojawiało się na „Wolne teraz" mimo że fizycznie wróciło. Teraz `busyToday` wyklucza status `completed`: oddane auto **natychmiast wraca** na listę wolnych (i schodzi z licznika „Zajęte dziś"). Zweryfikowane live: po zwrocie od [klient 375] „Wolne teraz" = 5 z 9 (Renault Master UNZ27 wskoczył na listę). Build webpack czysty.

## 2026-07-15 (27) — Dashboard: zakończony zwrot widać jako odhaczony
- Po „Zapłacono → zakończ" (rozliczenie km) zwrot rezerwacji w „Wydania i zwroty" pokazuje się teraz jako **„Zwrócono ✓"** — zielony ptaszek zamiast strzałki + przekreślona nazwa + zielona plakietka. Wcześniej wyglądał identycznie jak niezrobiony (mimo statusu `completed` w bazie); znikał tylko z „Zaległych zwrotów". Teraz od razu widać na dashboardzie, że najem jest domknięty. Zweryfikowane live ([klient 691] / Renault Master). Build webpack czysty.

## 2026-07-15 (26) — Dashboard: klik w zwrot → rozliczenie km
- W sekcji „Wydania i zwroty" (Dziś/Jutro) klik w **zwrot rezerwacji** otwiera teraz okno **„Rozlicz km i zakończ"** (licznik PO → km × 7 ISK → QR/link Revolut → „Zapłacono → zakończ" zapisuje stan licznika i zamyka najem) zamiast edycji wpisu. Zwroty blokad/serwisów dalej otwierają edycję. Zweryfikowane live: klik w dzisiejszy zwrot ([klient 691], Renault Master) otwiera rozliczenie km (nie kreator edycji). Build webpack czysty.

## 2026-07-15 (25) — Checklista: wyposażenie dużego żółtego kampera
- Nowa sekcja na Checklistcie **„Wyposażenie — duży kamper (żółty)"** (18 pozycji: śpiwór/poduszka/poszewka/ręcznik per osoba, plastikowy zbiornik, talerze duże/małe, łyżki, widelce, garnuszki, patelnia, kawiarka, kubeczki, nabój w kuchence + zapas, lodówka, **stół, krzesełka**). Odhaczane per klient — stan zapisuje się w bazie tak samo jak checklista wydania (klucze `eq_*` w `customer_checklists`). Widoczne też w szybkiej checklistcie poglądowej. „Odznacz wszystko" czyści też wyposażenie. Zweryfikowane live: sekcja + 16 pozycji + odhaczanie działa. Build webpack czysty.

## 2026-07-15 (24) — Dashboard: „Wolne teraz" (auta dostępne dziś)
- Nowa sekcja na dashboardzie **„Wolne teraz (X z Y aut)"** — lista aut zdatnych do wynajmu (status aktywny, nieukryte), które NIE mają dziś żadnego wpisu (rezerwacja/blokada/serwis obejmujący dziś), z **nazwą i rejestracją**. Auto zajęte dziś (np. Trafic ROB64 na wynajmie do 21.07) na niej nie figuruje. Zweryfikowane live: „4 z 9 aut" (Daci Duster Namiot PFS75, Dacia Duster - górki FZZ82, Dacia Duster Namiot II BPS82, Renault Master II RHR89). Build webpack czysty.

## 2026-07-15 (23) — Kreator: koniec „klientów-sierot" + Trafic przedłużony do 21.07
- **Nowy klient w kreatorze zapisuje się DOPIERO przy „Potwierdź", razem z rezerwacją.** Wcześniej „Nowy klient" tworzył klienta natychmiast, więc wyjście z kreatora bez zapisania rezerwacji zostawiało klienta-sierotę / duplikat — to była przyczyna pustego duplikatu „Biuro Podróży…". Teraz dane nowego klienta są trzymane lokalnie (Podsumowanie pokazuje „Nowy klient — zapisze się przy Potwierdź"), a klient + rezerwacja powstają na końcu; wybór istniejącego klienta kasuje wpis oczekujący. **Zweryfikowane live:** „Nowy klient" → wpisanie danych → zamknięcie kreatora bez Potwierdź = **0 nowych klientów w bazie** (wcześniej: sierota). Build webpack czysty.
- **Dane:** rezerwacja Trafic (Biuro Podróży / Ladies trip) przedłużona w bazie **20.07 → 21.07** (zgodnie z ręcznym dopiskiem na podpisanej umowie), **cena bez zmian** (156 240 ISK), 0 kolizji. Pusty duplikat klienta „Biuro Podróży…" usunięty (guarded, 0 zależności) — rezerwacja nietknięta.

## 2026-07-15 (22) — Kalendarz: notatka na pasku rezerwacji (żeby się nie myliły)
- Pasek rezerwacji na kalendarzu pokazuje teraz **nazwę klienta + początek notatki** — np. „Biuro Podróży Włóczykij Dorota Sawa — [klient 127] **· Ladies trip [tel. usuniety] [klient 021]**", „[klient 152] · Wydanie 26.08… WhatsApp". Dzięki temu rezerwacje dało się odróżnić i się nie mylą. Blokady bez zmian (pokazują notatkę jak dotąd, bo nie mają klienta). Za długie ucina CSS; pełna treść jest w tooltipie (hover). Zweryfikowane live.

## 2026-07-15 (21) — Klient firmowy widoczny po nazwie firmy (płatnika)
- **Rezerwacje firmowe pokazują się nazwą FIRMY (najemcy/płatnika), nie nazwiskiem korzystającego.** Przy kliencie firmowym firma płaci, a osoba (np. pilot wycieczki) jeździ. Apka pokazywała takie rezerwacje nazwiskiem kierowcy → nie dało się ich znaleźć po nazwie biura. Nowy wspólny helper `customerLabel` (klient firmowy → „Firma — Korzystający") podstawiony w: kalendarzu (pasek + wyszukiwarka), drawerze wpisu, liście Rezerwacje (+ szukajka + sort), liście Klienci, dropdownie umowy, checkliście oraz kreatorze (szukajka klientów + podgląd) i na dashboardzie (Dziś/Jutro/zaległe). Zweryfikowane live: rezerwacja **Trafic ROB64** czyta się teraz „Biuro Podróży Włóczykij Dorota Sawa — [klient 127]", a wpisanie „Biuro Podróży" w wyszukiwarce kalendarza ją znajduje.
- **Kontekst (śledztwo „gdzie zniknęła rezerwacja"):** rezerwacja „Lejdis trip" wyglądała na zgubioną, bo w bazie był też **pusty duplikat** klienta „Biuro Podróży Włóczykij Dorota Sawa" (0 rezerwacji/dokumentów/umów), a właściwa rezerwacja siedziała pod pełnym rekordem pokazywanym jako „[klient 127]" (firma płatnik + korzystający + dokumenty). **Zero utraty danych** — booking `d64a4f8d`: Trafic, 14–20.07.2026, 22 320 ISK/doba = 156 240 ISK. Ta zmiana dotyczy wyłącznie WYŚWIETLANIA; żadnych danych nie ruszano. (Pusty duplikat do usunięcia — czeka na potwierdzenie Kamila.)
- Build (webpack) czysty (19 tras).

## 2026-07-15 (20) — Naprawa okienek (CSP), rozliczenie km, licznik na umowie, scroll checklisty
- **Fix: okienka „Dane klienta / Umowa / Checklista" znów działają.** CSP `frame-src` miał tylko `accounts.google.com` (od dodania nagłówków dla `/book`) — bez `'self'` przeglądarka **blokowała wszystkie** iframe własnych stron apki (stąd „broken frame", żaden przycisk nie działał). Dodane `'self'`. Zweryfikowane w przeglądarce: przed = `Framing '…/customers?embed=1' violates CSP directive frame-src`, po = ładuje się.
- **„Dane klienta" = natywny modal (nie iframe).** Przycisk w drawerze kalendarza otwiera teraz **ten sam pełny formularz co w Klientach/kreatorze** (firma/osoba, adres, PESEL, dowód/prawko z datami) wypełniony danymi klienta i jego dokumentami — zapis przez `updateCustomer` + wspólny reconcile dokumentów. Szybsze, pewne, bez zależności od iframe. „Umowa" i „Checklista" zostają jako (już działające) okienka.
- **Rozliczenie kilometrów + zamknięcie rezerwacji.** Nowy przycisk **„Rozlicz km i zakończ"** (rezerwacje) → okienko: licznik PRZED (podpowiedziany) + PO, liczy **(PO − PRZED) × 7 ISK**, pokazuje **QR i link Revolut** na tę kwotę, a **„Zapłacono → zakończ"** zapisuje stan licznika i ustawia status **Zakończona**.
- **Umowa: stan licznika.** Główna umowa pokazuje teraz **„Stan licznika przy wydaniu (km)"** (i przy zwrocie) w tabeli przedmiotu — wcześniej przebieg był tylko na protokołach wydania/zwrotu.
- **Kreator: po „Zapisz klienta" od razu Podsumowanie.** Nowo utworzony klient jest wybrany i kreator przechodzi na krok Podsumowanie (koniec ponownego wybierania). Cichy błąd zapisu dokumentu → toast (dane nie znikają po cichu).
- **Fix scrolla (checklista i inne strony po użyciu modali).** `useModalChrome` przy nakładających się modalach potrafił zostawić `body{overflow:hidden}` na stałe (znikało przewijanie). Zamieniony na **globalny licznik** otwartych modali — tło odblokowuje się dopiero po zamknięciu ostatniego, niezależnie od kolejności.
- **Fixy z code-review VAT:** (1) zamiana rezerwacji na blokadę **czyści** `vat_rate` (`null`, nie `undefined`) — koniec nieaktualnej stawki przy zerowym VAT; (2) **blok→rezerwacja domyślnie 24%** (nowy najem opodatkowany), 0% tylko przy edycji istniejącej rezerwacji bez VAT; (3) umowa **nie twierdzi „netto"** nad starymi (możliwe brutto) cenami z RentHelp — rozbicie netto/VAT pokazuje tylko gdy znana stawka VAT>0, inaczej neutralnie „kwota / doba"; (4) modal klienta nie zawiesza się na „Zapisywanie…" gdy zapis rzuci (`try/finally`); (5) wspólny helper `reconcileCustomerDocuments` (kreator + profil + kalendarz) — daty prawka bez numeru już nie giną.
- Bez utraty danych: zmiany addytywne (nowy modal/przycisk/kolumny umowy, nagłówek CSP, licznik scrolla). Rozliczenie km zapisuje tylko `odometer_*` + `status`. Build (webpack) czysty. Wnętrze za hasłem — klik-test przez Kamila.

## 2026-07-15 (19) — Oferta netto→VAT→brutto + pełny formularz klienta w kreatorze
- **Oferta: cena netto → VAT (VSK) → brutto.** W kroku „Oferta" pole to teraz **cena netto za dobę**, pod nim przełącznik **24% (standard) / 0% (zw./eksport)**, a podsumowanie rozbija kwotę na **Netto (stawka × dni) + VAT + Suma całkowita brutto**. Zapisywana jest stawka VAT (`vat_rate`) i **brutto** jako suma. VAT dotyczy tylko rezerwacji — blokady/serwis liczą się jak dotąd (netto, bez VAT). Nowa rezerwacja startuje z 24%; **edycja starego wpisu bez zapisanego VAT startuje z 0%**, żeby nie zawyżyć zsynchronizowanej z RentHelp kwoty (brutto zostaje = dotychczasowe). Umowa pokazuje teraz „Wartość netto za okres" + linię „w tym VAT (VSK) X%: … · wartość netto: …" + „Kwota całkowita do zapłaty" (brutto).
- **Migracja `20260715090000_booking_vat.sql`** — dodana kolumna `rental.bookings.vat_rate integer` (wdrożona na prod przez Management API: 84 rezerwacje, **0 tkniętych** — kolumna pusta, żadna zapisana kwota nieprzeliczona). Mapowanie w `db.ts` (insert/update/read) + w `seed.sql`.
- **Kreator: pełny formularz nowego klienta.** „Nowy klient" w kroku Klient otwiera teraz **ten sam pełny modal co w zakładce Klienci** (`CustomerFormModal`) — firma vs osoba prywatna (sekcja danych firmy), adres, PESEL, **dowód/paszport + prawo jazdy z datami wydania i ważności** — zamiast okrojonych 3 pól (imię/tel/e-mail). Dane wpisywane raz, komplet, także przy przepisywaniu blokady na wynajem. Dokumenty lądują w osobnej tabeli (jak w Klientach), po zapisie klient jest od razu wybrany do rezerwacji.
- Bez utraty danych: zmiany wyłącznie addytywne (nowa kolumna, nowe pola formularza). Build (webpack) czysty, aplikacja bootuje bez błędów (login 200, zero błędów konsoli). Wnętrze kreatora/umowy — za hasłem — do klik-testu przez Kamila.

## 2026-07-14 (18) — flow potwierdzeń domknięty + fix flasha danych
- **Wysłanie potwierdzenia odznacza rezerwację** (`7dc4052`). Po kliknięciu „Akceptuj — wyślij" w okienku status sam przechodzi wstępna→potwierdzona (tylko wstępne; aktywnych/zakończonych nie dotyka), a modal dociąga świeże dane — wpis znika z kafla „Do potwierdzenia" bez odświeżania. Zasada bez zmian: **mail NIGDY nie wychodzi bez okienka z podglądem i Twojego kliku** (walidator) — dotyczy wszystkich wejść (auto po utworzeniu, dashboard, Rezerwacje, kalendarz).
- **Dashboard: przycisk „Potwierdź" w kaflu Do potwierdzenia** (`f848ea5`). Jeden klik → okienko wysyłki; zweryfikowane na produkcji wpisem testowym (usuniętym po teście).
- **Kreator: dropdown „Status rezerwacji"** (`f45f34f`) na kroku Termin i pojazd — domyślnie **Potwierdzona** (dogadka na messengerze = 95% pewna; mail leci od razu po zapisaniu), **Wstępna** wybieralna w razie czego. Przy edycji wpisu w innym stanie (aktywna/zakończona/anulowana) status jest pokazywany i nie zmienia się po cichu; edycja blokady/serwisu statusu nie dotyka.
- **Fix: koniec „flasha" starych danych przy odświeżeniu** (`f15f2f2`). Panel startował od wbudowanego snapshotu bazy z 1 lipca i po chwili podmieniał na żywe dane (stare wpisy pojawiały się i znikały). Teraz: pusty start + ekran „Wczytywanie danych…". Bonus bezpieczeństwa: snapshot (dane 75 klientów) wypadł z klienckiego bundla JS — potwierdzone na produkcji (0 trafień w chunkach).
- **Dane (decyzje Kamila):** 5 rezerwacji z niejednoznacznym kursem (RH-1065/1066/1072/1090/1091) → wpisane 0 jako placeholder do późniejszej poprawki w kreatorze; [klient 416] (RH-1105) → zwrócona (nie zalegała); rezerwacja [klient 292] → potwierdzona w bazie (mail wysłany ręcznie przed fixem statusu). Wstępnych w bazie: 0, zaległych: 0, pokrycie cen: 37/59.
- **Reguła zapisana w kodzie (bez zmiany zachowania — tak już działało):** liczba dni najmu liczona WYŁĄCZNIE z zakresu dat (od–do), +1 = dzień zwrotu wliczony. Godziny wydania/odbioru są tylko informacyjne (dashboard / umowa / drawer) i NIE wpływają na liczbę dni ani na kwotę. Komentarz-guard dodany przy obu wyliczeniach dni (kreator `NewReservationWizard.tsx`, umowa `contract.ts`).

## 2026-07-13 (17) — nocna sesja: ceny z RentHelp + fixy audytu + twardy constraint
- **Ceny z RentHelp zsynchronizowane (PLN = ISK).** Odkryty wewnętrzny API RentHelpa (`/api/gateway/reservation-aggregates`) i przeskanowane wszystkie 36 rezerwacji. Kwoty wpisywane tam „w zł" to ISK; wykryte dwa pewne style zapisu (1:1 oraz kurs 1 ISK = 1 grosz — zdradzają go końcówki ,29/,67/,58) i jeden niejednoznaczny (realny kurs — 5 rezerwacji POMINIĘTE do decyzji: 1065 [klient 404], 1066 [klient 375], 1072 [klient 353], 1090 [klient 342], 1091 [klient 305]). **Uzupełnione 30 rezerwacji** (stawka+suma+kaucja, tylko puste pola, zero nadpisań) + **14 blokad** (stawka z notatki). Pokrycie cen: 2/59 → 32/59; przychód lipca na dashboardzie: 175 000 → **1 037 500 ISK** (6/6 wycenione). [klient 608] otagowana `RH-1107` + kaucja, telefon klientki uzupełniony.
- **Dashboard: sekcje Dziś + Jutro, klikalne wpisy, uczciwy przychód** (deploy `0019b2b`). Każdy wpis (Dziś/Jutro/Do potwierdzenia/Najbliższe 7 dni/Zaległe zwroty) otwiera edycję; kafel przychodu dostaje podpis „tylko X z Y rezerwacji ma wpisaną cenę", gdy dane są niepełne. Pajero (Blue/Silver) i Vito ukryte z pulpitu i licznika (dalej są w kalendarzu) — `hiddenVehicles.ts`.
- **Zaległe zwroty: sekcja działa + porządek w historii.** Nowa czerwona sekcja z „Oznacz jako zwrócone". Backfill: 39 dawno zakończonych najmów z importu (status `confirmed`/`active`, koniec przed 12.07) oznaczono `completed` — dashboard pokazuje tylko realne zaległości (dziś: 1 — [klient 416], zwrot 12.07).
- **Twardy constraint anty-double-booking NA BAZIE (`bookings_no_overlap`).** EXCLUDE (GiST, daterange `[)`, bez anulowanych, podłoga 2026-01-01 — historia importu 2024/25 nietknięta). Zweryfikowany na żywo: kolizyjny insert odrzucony (23P01), styk zwrot/odbiór tego samego dnia przechodzi. Aplikacja łapie 23P01 i pokazuje „Termin zajęty".
- **Spójna granica dat we wszystkich 3 warstwach.** `hasOverlap` (admin), publiczny submit (`book-public.ts`) i constraint DB używają half-open `[start,end)` — zmiana auta tego samego dnia (zwrot rano / wydanie po południu) nie jest już fałszywą kolizją nigdzie.
- **Mail potwierdzenia: płatność przy odbiorze + kontakt Messenger/WhatsApp** (bez bloku Revolut; Revolut zostaje w mailu „Płatność za rezerwację" i w panelu). Fix kwoty rezerwacji [klient 292]: 225 000 → 250 000 (10 dni × 25 000, liczenie inclusive).
- **Fixy audytu wdrożone (8):** `total_price` zapisywany przy potwierdzaniu wniosku self-service (przychód nie gubi rezerwacji z linka); read-path fail-closed (produkcja bez bazy → ekran błędu z „Spróbuj ponownie" zamiast cichego, zamrożonego seeda z PII); miejsce wydania/odbioru z kreatora zapisywane do bazy i na umowę (kolumna `location`, migracja wdrożona); numery umów nadawane atomowo serwerowo + UNIQUE index (migracja wdrożona) — koniec ryzyka dwóch umów z tym samym numerem; nagłówki bezpieczeństwa (HSTS, CSP, X-Frame-Options, nosniff, Referrer-Policy) na wszystkich trasach; drawer kalendarza ze scroll-lock + Escape + `role=dialog`.

## 2026-07-13 (16) — domknięcie produkcyjne (audyt→fixy)
- **Twarda blokada podwójnej rezerwacji.** Kreator blokuje zapis przy kolizji (jawny checkbox „zapisz mimo kolizji"); serwer re-sprawdza nakładanie w `insertBooking`/`updateBooking` (flaga `force`) i w potwierdzaniu wniosku self-service (termin mógł zostać zajęty). `hasOverlap()` = ta sama logika co kalendarz/publiczny submit.
- **AC (kasko) + alerty wygaśnięć.** Dodana kolumna `insurance_ac_expiry` (migracja `20260713150000`, wdrożona) — była gubiona. Dashboard: kafel „Ubezpieczenia i przeglądy" dla OC/AC/przeglądu wygasających ≤30 dni lub po terminie.
- **Niezawodność maili + odświeżanie.** Decyzje we Wnioskach zwracają `emailSent` → toast ostrzega „mail NIE wyszedł" zamiast fałszywego sukcesu. Po potwierdzeniu `DataProvider.refresh()` dociąga nowego klienta+rezerwację (były niewidoczne do reloadu).
- **Flota: wyszukiwarka + „Dodaj pojazd".** Spójnie z resztą (fold PL). Usuwanie pominięte celowo (kaskadowo kasuje rezerwacje).
- **Modale: blokada scrolla tła (`useModalChrome`) + `role=dialog`.**
- **Higiena danych (prod).** Usunięto 4 rekordy bez zależności: 2 duplikaty ([klient 380], [klient 221] — wariant wielkości liter e-maila) + 2 testowe ([klient 154], „[klient 798]"). Zostało 74 klientów, zero duplikatów nazw. **Do decyzji Kamila:** `[e-mail klienta usuniety]` = [klient 828] + [klient 581] (ten sam telefon, każdy z 1 rezerwacją) — scalić czy to dwie osoby? oraz klient testowy „[klient 443]" ma realną rezerwację (VW Caddy California, 11–23.07) — przemianować na prawdziwą osobę. Po tym można dodać unikalny indeks na e-mail.
- Security-review na diffie: brak exploitów; atomowy claim + re-check nakładania utwardzają wyścig.

## 2026-07-13 (14) — audyt: P0 XSS + legal + kolejne fixy
- **Fix P0 — stored XSS w umowach.** Dane z PUBLICZNEGO formularza (`/api/book`: nazwisko, adres, dokument, prawo jazdy, firma) trafiały nieescape'owane do HTML umowy renderowanego przez `dangerouslySetInnerHTML` w sesji admina → nazwisko `<img onerror=…>` wykonywało kod w panelu (kradzież cookie sesji). `contract.ts` escape'uje teraz każdą wartość klienta/dokumentu (`najemcaBlock` + końcowe podstawianie, poza gotowym blokiem HTML). Zweryfikowane: payload neutralizowany, polskie/islandzkie znaki nietknięte.
- **Fix legal — umowa nie zmyśla „0 ISK".** Kaucja i udział własny, gdy nie zostały wpisane, dają teraz pustą linię do uzupełnienia zamiast fałszywego „0 ISK" (wcześniej każda umowa deklarowała 0 ISK udziału i 0 ISK kaucji — ekspozycja przy roszczeniu).
- **Fix — publiczny link nie wycieka rządowego ID.** `/api/book/[token]` zwracał w `prefill` numer dowodu i prawa jazdy dla dowolnego tokenu, też wygasłego/rozstrzygniętego. Teraz prefill tylko dla aktywnego linku i BEZ numeru dokumentu/prawa jazdy (klient wpisuje sam).
- **Fix — dashboard: przychód bez „wstępnych".** Kafelek przychodu wykluczał tylko anulowane; teraz pomija też `tentative` (spekulacyjne holdy), żeby liczba nie była zawyżana.
- **Fix — notatka w kalendarzu: koniec fałszywego sukcesu.** Zapis notatki sprawdza teraz wynik `updateBooking` (przy błędzie DB nie pokazuje „zapisano").
- **Fix mobile — inputy 16px.** 7 formularzy/wyszukiwarek (Klienci, Rezerwacje, Checklista, modale klienta/pojazdu, profil, Wnioski) miało 14px → iOS zoomował przy focusie. `text-base md:text-sm` (16px na telefonie, 14px na desktopie) — ważne, bo ekipa wypełnia to przy wydaniu auta.

## 2026-07-13 (13) — audyt: naprawy P1
- **Fix P1 (dedup po e‑mailu).** W potwierdzaniu wniosku (`decideBookingRequest`) dedup klienta używał `.eq(email).maybeSingle()`, które **sypie błędem, gdy e‑mail się dubluje** (a takie są w bazie) → istniejący klient był ignorowany i powstawał kolejny duplikat dokładnie dla już zduplikowanych. Teraz bierzemy pierwszy pasujący rekord tablicą (`order+limit(1)`), bez błędu.
- **Fix P1 (wyścig podwójnego potwierdzenia).** `decideBookingRequest`/confirm robił read‑then‑write bez atomowej blokady — dwa kliknięcia/dwóch adminów tworzyło dwa duplikaty klienta+rezerwacji i dwa maile. Dodane atomowe „zajęcie" (`update ... where id=? and status='submitted' → 'confirming'`, sprawdzenie liczby wierszy) + cofnięcie do `submitted` przy błędzie.
- **Fix P1 (fail‑closed `APP_AUTH_SECRET`).** Bez sekretu kod po cichu używał publicznego `dev-insecure-secret-change-me` → sesje do podrobienia = obejście panelu. Teraz w produkcji rzuca zamiast użyć domyślnego. Zweryfikowane na żywo, że prod ma ustawiony własny sekret (forged cookie odrzucony), więc zmiana nic nie psuje.

## 2026-07-13 (12) — potwierdzenie rezerwacji na e‑mail
- **Wysyłka potwierdzenia rezerwacji na e‑mail — ręcznie i automatycznie.** Nowe okienko `SendConfirmationModal`: **podgląd treści maila** (iframe), **edytowalny adres odbiorcy** („popraw email") i akcje **Akceptuj — wyślij / Odrzuć**. Dostępne z **kalendarza** (przycisk „Wyślij potwierdzenie e‑mail" w Szczegółach wpisu) i z **Rezerwacji** (ikona maila w wierszu). Po **utworzeniu nowej rezerwacji z klientem mającym e‑mail** okienko otwiera się od razu (auto‑prompt). Mail = ten sam szablon potwierdzenia co self‑service (z QR Revolut + kwotą). Serwer: `prepareBookingConfirmation` (podgląd) + `sendBookingConfirmation` (wysyłka na podany adres).

## 2026-07-13 (11)
- **Fix: QR/statyki spod Basic Auth (mail z QR do klienta był zepsuty).** Weryfikacja na żywo na produkcji wykazała, że `/revolut-qr.png` (a także ikony PWA, manifest) zwraca **401 Basic Auth** — więc kod QR w mailu potwierdzenia/płatności do klienta **nie ładował się** (klient nie ma haseł zespołu). `proxy.ts` wyłącza teraz pliki statyczne (po rozszerzeniu: png/jpg/svg/ico/webmanifest/js/json/…) spod Basic Auth i spod redirectu sesji — są niewrażliwe, a muszą być publiczne dla maili i PWA. `/book/*` i `/api/book/*` publiczne bez zmian; strony i API panelu nadal za bramką. (Produkcyjny alias publiczny: `mountain-car-rental.vercel.app`; per-deploy URL-e są za Vercel SSO.)

## 2026-07-13 (10)
- **Blokady funkcjonują jak rezerwacje.** Zdjęte ograniczenie „tylko typ=rezerwacja" z akcji w „Szczegóły wpisu" — umowa, płatność (Revolut/QR), wysyłka płatności e-mailem, dane klienta i checklista zależą teraz tylko od tego, czy jest przypięty klient, nie od typu wpisu. Dzięki temu blokada z przypiętym klientem ma pełen zestaw funkcji rezerwacji. Wpisy bez klienta (m.in. importowane blokady RentHelp z klientem w notatce) mają przycisk **„Przypisz klienta (odblokuj funkcje rezerwacji)"** — po przypięciu (z podpowiedzią imienia/stawki z notatki) dostają wszystko.

## 2026-07-13 (9)
- **„Zamień na rezerwację" dla blokad z RentHelpa.** Importowane wpisy typu Blokada z klientem i ceną w notatce (np. „[klient 691] messenger 25000") nie miały funkcji rezerwacji (brak `type=reservation` i przypiętego klienta → brak klienta/umowy/checklisty/płatności). W „Szczegóły wpisu" blokady/serwisu jest teraz przycisk **„Zamień na rezerwację"** — otwiera kreator z typem = Rezerwacja i **podpowiedzianym imieniem klienta oraz stawką wyciągniętą z notatki** (fold liczb/słów typu „messenger"; zweryfikowane na realnych wpisach). Po przypięciu klienta wpis zyskuje wszystkie funkcje rezerwacji. Podpowiedzi są edytowalne w kreatorze.

## 2026-07-13 (8)
- **Rezerwacje: klikalny pojazd i klient w wierszu.** Klik w nazwę pojazdu → podgląd/edycja tego wpisu (kreator). Klik w nazwę klienta → jego profil w okienku (IframeModal, jak na kalendarzu — bez wychodzenia z listy). Wpisy bez klienta (blokady) pokazują opis jako zwykły tekst. Ikonka ołówka do edycji zostaje.

## 2026-07-13 (7)
- **Notatka wewnętrzna per rezerwacja — szybka edycja z kalendarza.** W „Szczegóły wpisu" notatka jest teraz edytowalnym polem (zapis do `bookings.notes`, przycisk „Zapisz notatkę" pojawia się po zmianie) — można coś dopisać bez otwierania pełnego kreatora. Notatki per klient już były na profilu („Notatki o kliencie — widoczne tylko dla zespołu"). Obie tylko do użytku wewnętrznego, nie widzi ich klient.

## 2026-07-13 (6)
- **Potwierdzenia „na pewno?" i podsumowania przed ważnymi akcjami.** Nowy reużywalny `ConfirmDialog` (tryb danger dla usuwania + opcjonalne wiersze podsumowania do sprawdzenia; Esc/klik zamyka, przycisk pilnuje stanu „w toku"). Wpięty w: **usuń wpis** na kalendarzu (podsumowanie: typ/pojazd/klient/termin), **usuń klienta** (zastąpił brzydki natywny `confirm()`; pokazuje liczbę rezerwacji), **usuń dokument** klienta, **Potwierdź** wniosku w Wnioskach (podsumowanie klient/e-mail/pojazd/termin + info że utworzy klienta+rezerwację i wyśle maila) oraz **Wyślij umowę** (podsumowanie klient/szablon/firma/numer/rezerwacja; ostrzega, gdy dane klienta niekompletne).

## 2026-07-13 (5)
- **Kalendarz: „Dane klienta / Umowa / Checklista" otwierają się w okienku, nie wyrzucają z widoku.** Nowy `IframeModal` pokazuje realną stronę apki (edycja klienta, generator umowy, checklista) w iframe na wierzchu kalendarza — powiązaną z bazą, z buttonem „otwórz w pełnym widoku" i zamknięciem (X / Esc). `AppShell` dostał tryb `?embed=1`: strona w iframe renderuje się bez menu bocznego i paska ładowania (spinner trzyma się do `onLoad`, więc bez migotania). Same-origin + cookie `SameSite=Lax` → strona w okienku jest zalogowana; brak nagłówków blokujących iframe.

## 2026-07-13 (4)
- **Naprawa nadawcy maili (root cause, dlaczego nie wychodziły).** Klucz Resend JEST w vaulcie (Infisical, MAS Group/dev, ścieżka `/mountaincar`: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `RESEND_TO_EMAIL`) — wcześniejszy „check" patrzył tylko na `/`, stąd fałszywy wniosek „brak klucza". Klucz zweryfikowany realną wysyłką (200) z `noreply@mountaincar.is` na `rental@mountaincar.is` — domena mountaincar.is jest zweryfikowana w Resend, klucz jest send-only i działa. Błąd był w kodzie: `email.ts` czytał `EMAIL_FROM`, a konfig używa `RESEND_FROM_EMAIL` → nadawca spadał na `onboarding@resend.dev` (Resend nie dowozi tego do klientów). Teraz `sendEmail` czyta `EMAIL_FROM || RESEND_FROM_EMAIL`. Pozostaje warunek: te 3 zmienne muszą być w env produkcyjnym Vercela (redeploy je podchwyci).

## 2026-07-13 (3)
- **QR Revolut + link płatności w mailach.** Mail potwierdzenia rezerwacji (Wnioski → Potwierdź) zawiera teraz blok płatności: kod QR (`/revolut-qr.png`, absolutny URL z origin), przycisk „Zapłać przez Revolut" i kwotę (stawka × dni, jeśli znana). Nowy szablon `emailPayment`.
- **Kalendarz → „Wyślij płatność e-mailem (Revolut + QR)".** W „Szczegóły wpisu" rezerwacji z klientem mającym e-mail nowy przycisk wysyła klientowi maila z QR i linkiem Revolut (akcja `sendPaymentEmail` — pobiera e-mail/pojazd/kwotę z bazy). Zwraca czytelny toast (sukces / brak e-maila / błąd Resend).
- ⚠️ **Delivery maili nadal wymaga klucza Resend** — w vaulcie/prod nie ma `RESEND_API_KEY`, więc `sendEmail` degraduje się po cichu (potwierdzenie klienta z testu z linkiem NIE wyszło z tego powodu; sam wniosek zadziałał — klient dodany, rezerwacja utworzona i widoczna w kalendarzu/rezerwacjach). Do włączenia: konto Resend + weryfikacja domeny (mountaincar.is) + `RESEND_API_KEY` (i `EMAIL_FROM`) w env produkcyjnym Vercela.

## 2026-07-13 (2)
- **Godzina wydania i odbioru na rezerwacji.** Nowe pola `pickup_time`/`return_time` (migracja `20260713100000`, wdrożona na prod; osobno od dat — logika kolizji i kalendarz bez zmian). Kreator (krok Termin, też edycja), szczegóły wpisu na Kalendarzu, tabela Rezerwacji (Od/Do), dashboard „Dziś — wydania i zwroty" i umowa (Data wydania/zwrotu z „godz. HH:MM"). Rezerwacji [klient 292] Kozłowskiego uzupełniono 01:00/18:00 z notatki.
- **Kalendarz → szybkie akcje z wpisu.** „Szczegóły wpisu" mają teraz: **Dane klienta (edytuj/uzupełnij)** → profil, **Wygeneruj umowę** (dawny „Podgląd umowy") i **Checklista wydania** → `/checklist?customerId=…` (deep-link istniał, teraz jest dojście jednym klikiem).
- **Wyszukiwarka w Rezerwacjach** — ta sama szeroka logika (fold PL): klient, pojazd, rejestracja, notatka oraz **typ i status po polsku** („blokada", „serwis", „wstępna"…).
- **Daty wydania/ważności dowodu i prawa jazdy wszędzie.** Formularz klienta (sekcje Dowód/Prawo jazdy z datami; zapis podmienia dokument, daty przenoszone), umowa i protokoły („ABC123 (wyd. 01.02.2020, ważny do 01.02.2030)" przy dokumencie tożsamości i prawie jazdy), publiczny `/book` (pola dat), Wnioski (podgląd z datami), a **potwierdzenie wniosku tworzy dokumenty na profilu nowego klienta** (dowód + prawko z datami).
- **Publiczny `/book`: dokumenty OBOWIĄZKOWE.** Numer dokumentu tożsamości i prawa jazdy + daty wydania i ważności wymagane — walidacja na froncie i serwerze (`book-public.ts`); nowe kolumny `client_id_issued/expires`, `client_license_issued/expires` (kopiowane też do linku „poproś o zmianę").
- **Sekcja zaufania na `/book`.** Pod kreatorem: szyfrowane połączenie, RODO (dane tylko do umowy najmu, nieprzekazywane dalej) i pełne dane firmy (Mountain All Service ehf., kennitala, adres, mountaincar.is, e-mail, telefon).

## 2026-07-13
- **Dane Rebel Travel ehf. uzupełnione na umowie** — kennitala `600723-0140` i VSK-nr `149557`. Kennitala odczytana z umowy generowanej przez RentHelp (blok Wynajmującego, rezerwacja #1107) i potwierdzona w fyrirtækjaskrá Skatturinn (firma aktywna, zarej. 17.07.2023, ISAT 77.11.0 — wynajem samochodów); VSK-nr z rejestru (potwierdzony dwukrotnie). Ostrzeżenie „kennitala i VSK-nr do uzupełnienia" przy wyborze Rebel Travel znika samo; na umowie zamiast pustych linii są numery.

## 2026-07-12
- **Naprawa „Drukuj / PDF" na Kontrakcie.** Wydruk ucinał się do jednej strony, bo podgląd umowy ma na ekranie `max-height:70vh` + przewijanie, a CSS druku tego nie zdejmował — PDF łapał tylko wysokość jednego ekranu. W `@media print` dochodzi `max-height:none`/`overflow:visible` (też na `html/body`) + marginesy stron `@page 14mm`. Zweryfikowane headless Chromem na replice strony: stary CSS → 1 strona bez końca dokumentu; nowy → 6 stron, pełna treść, interfejs apki ukryty.
- **Pole „Dowód osobisty (numer)" w formularzu klienta** (Nowy klient i Edytuj klienta) — obok PESEL i prawa jazdy. Zapisuje się w istniejącym systemie dokumentów klienta (typ „Dowód osobisty"), więc umowa od razu podstawia go w linii „Dokument tożsamości", a ostrzeżenie „Brakujące dane klienta" na Kontrakcie znika. Przy edycji pole pokazuje obecny numer; zmiana podmienia wpis (daty wydania/ważności przenoszone); wyczyszczenie pola celowo NIE usuwa dokumentu (od tego zakładka Dokumenty).

## 2026-07-12
- **Wybór firmy na umowie (Wynajmujący): Mountain Car albo Rebel Travel.** Na stronie Kontrakt nowy przełącznik (dwa przyciski, domyślnie Mountain Car — jak dotąd). `contract.ts` ma teraz `COMPANIES[]` z dwoma profilami; `buildFilled` przyjmuje wybraną firmę i podstawia jej dane we wszystkich szablonach (umowa + protokoły wydania/zwrotu). Rebel Travel zweryfikowane ze źródeł (rebeltravel.is + RentHelp): nazwa „Rebel Travel ehf", adres Skógarhlíð 10, 105 Reykjavík, e-mail info@rebeltravel.is, web rebeltravel.is. **Kennitala i VSK-nr Rebel Travel do uzupełnienia** — nie znaleziono ich w RentHelp ani w publicznym rejestrze, a zmyślonych numerów na umowę prawną nie wpisuję; póki puste, renderują się jako linie do ręcznego uzupełnienia + ostrzeżenie w UI. Zawartość umowy zapisuje wybraną firmę (jest w treści HTML), więc podpięta na profilu klienta odzwierciedla właściwego Wynajmującego.

## 2026-07-10 (wieczór, 6)
- **Karta Revolut z kodem QR w Ustawieniach.** Wcześniej był tam tylko wiersz tekstowy z linkiem — teraz pełna karta `RevolutPay` (kod QR + link + „Kopiuj"/„Wyślij przez WhatsApp"), ta sama co na Checkliście i przy rezerwacji. Zduplikowany wiersz tekstowy usunięty.

## 2026-07-10 (wieczór, 5)
- **Prawdziwy kod QR Revolut** (firmowy, z logo R i @m_krokoszynska) zamiast generowanego. Screenshot „Mój kod" przycięty do samego QR (bez chromu telefonu i przycisków Google Wallet), zapisany jako `public/revolut-qr.png` i pokazywany w komponencie `RevolutPay`. Usunięta zależność `qrcode-generator` (już niepotrzebna). To ten sam kod, który klienci realnie skanują.

## 2026-07-10 (wieczór, 4)
- **Szybka checklista poglądowa (bez klienta).** Na stronie Checklista, gdy nie wybrano klienta i nie trwa wyszukiwanie, pokazuje się „Szybka checklista (poglądowa)" z tymi samymi 12 punktami. Stan trzymany tylko w oknie (`useState`) — **resetuje się przy odświeżeniu / ponownym wejściu**, nic nie zapisuje do bazy (w przeciwieństwie do wersji per klient). Pasek postępu + „Wyczyść". Render pozycji wyciągnięty do wspólnego komponentu `ChecklistGroups`, używanego przez obie wersje (per klient i poglądową).

## 2026-07-10 (wieczór, 3)
- **Płatność Revolut — łatwo podać klientowi.** Klienci płacą głównie przez Revolut, więc dodany reużywalny komponent `RevolutPay` (`src/components/RevolutPay.tsx`) z linkiem `revolut.me/m_krokoszynska` (@m_krokoszynska): **kod QR** do zeskanowania na miejscu (generowany offline biblioteką `qrcode-generator`, bez wywołań sieciowych), **„Kopiuj link"** i **„Wyślij przez WhatsApp"** z gotową wiadomością (z imieniem klienta i kwotą; `wa.me/<numer>` gdy znamy telefon). Umieszczony: pełna karta na **Checklista** (przy wybranym kliencie, kwota z jego ostatniej rezerwacji), kompaktowy przycisk „Poproś o płatność" w **szczegółach wpisu na Kalendarzu**, oraz wiersz w **Ustawieniach**. Odbiorcę zmienia się w jednej linii (`src/lib/payment.ts`). QR + helpery zweryfikowane (poprawny data-URL, `wa.me` z numerem i bez).

## 2026-07-10 (wieczór, 2)
- **Nowa zakładka „Checklista" — lista wydania auta przypięta do klienta.** W menu (`/checklist`) szeroka wyszukiwarka klientów (ta sama logika co w Klientach/Kalendarzu: imię/nazwisko/telefon/e-mail/pojazd/rejestracja + fold PL znaków); po wyborze klienta pokazuje się checklista w grupach (Przed wydaniem → Pokaz i obsługa auta → Instrukcje dla klienta → Po zwrocie) z 12 punktami wg listy Kamila (umowa, licznik PRZED/PO, kuchenka, lodówka, łóżko, zasłonki Caddy, kawa+kluczyki, grząski grunt, „auto kolegi Arka", kontrolka/mechanik po tripie, mapa-dodatek). Odhaczanie zapisuje się **per klient w bazie** (`rental.customer_checklists`, migracja `20260710190000`, RLS authenticated-only, wdrożona na prod) — stan współdzielony, widoczny na każdym telefonie ekipy. Optymistyczny zapis z rollbackiem + toast, pasek postępu (X/12), „Odznacz wszystko" (nowy trip tego samego klienta), skróty „Przygotuj umowę"/„Profil". Deep-link `?customerId=`. Warstwa zapisu (upsert onConflict + odczyt) zweryfikowana na prod.

## 2026-07-10 (wieczór)
- **Szeroka wyszukiwarka w Kalendarzu i w Klientach.** Wspólny helper `src/lib/search.ts` (`fold()` + `matchesQuery()`): spłaszcza polskie znaki i diakrytyki (`kozlowski` = `[klient 031]`, `asa` = `Ása`) i dopasowuje token-AND (każde słowo zapytania musi trafić, więc „kozlowski szymon" działa jak „szymon"). **Klienci** — pole nad tabelą filtruje po imieniu/nazwisku, telefonie, e-mailu, źródle, nr dokumentu/prawa jazdy, adresie, danych firmowych **oraz po pojazdach/rejestracjach, którymi klient jeździł** (z jego rezerwacji); licznik „X z Y" + pusty stan. **Kalendarz** — pole nad siatką: pusty = wszystko jak dotąd; z frazą pokazuje pojazd, gdy pasuje sam pojazd (nazwa/rejestracja) albo ma wpis pasujący po kliencie/notatce, a w wierszu bez trafienia pojazdu zostawia tylko pasujące paski; pusty stan „Brak wyników". Logika fold zweryfikowana (8/8 przypadków, w tym ł/diakrytyki/rejestracja).

## 2026-07-10 (po południu)
- **Czas islandzki w całej apce.** „Dzisiaj" i bieżący miesiąc liczą się teraz wg czasu islandzkiego (Islandia = UTC cały rok), nie wg strefy przeglądarki — nowe helpery `todayISO()`/`nowIceland()` w `dates.ts`, użyte w kalendarzu (Timeline), dashboardzie, rezerwacjach (filtr miesiąca), flocie (wygasające OC/przeglądy + utylizacja), kreatorze rezerwacji, publicznym `/book` i dacie zawarcia umowy. Wcześniej wieczorem (PL = +1/+2h) „dzisiaj" przeskakiwało na jutro względem Keflavíku.
- **Stan licznika na rezerwacji (rozliczenie km z urzędem).** Nowe pola `odometer_start`/`odometer_end` na bookings (migracja `20260710150000`, wdrożona na prod) + inputy „Stan licznika (km) — przy wydaniu / przy zwrocie" w kreatorze (krok Oferta, także przy edycji). Protokoły wydania/zwrotu w Kontrakcie podstawiają wpisany przebieg automatycznie (pusta linia gdy brak).
- **Numer umowy i data zawarcia nadają się same.** `makeNumber()` był losowy (####/07/2026); teraz to kolejny numer w bieżącym miesiącu wg czasu islandzkiego (`01/07/2026`, `02/07/2026`…), liczony z zapisanych umów w bazie. Podgląd pokazuje nadany numer przed wysłaniem; „zawarta dnia" = dzisiejsza data islandzka. Tabela umów była pusta, więc numeracja startuje czysto.
- **Weryfikacja waluty:** cała apka konsekwentnie w ISK (kreator, flota, kalendarz, umowa — `isk()`), zero śladów zł/PLN. Ciekawostka z porównania: konto RentHelp miało domyślną walutę rezerwacji ustawioną na **zł**.
- **Porównanie 1:1 procesu dodawania z RentHelpem** (screenshoty przez zalogowaną sesję, szkic porzucony bez zapisu): struktura kroków identyczna (Podstawowe dane→Oferta→Klient→Podsumowanie). Różnice: RentHelp twardo blokuje zajęte terminy (my: admin dostaje ostrzeżenie, publiczny /book blokuje twardo), ma godziny wydania/odbioru, taryfy, dzienny limit km + zł/km za przekroczenie, udział własny, dodatki i rabaty; my mamy za to stan licznika, typy wpisów (blokada/serwis) i self-service linki. Szczegóły w raporcie z sesji.

## 2026-07-10
- **Self-service booking links (klient sam rezerwuje).** Zespół generuje jednorazowy, wygasający po 60 min link do rezerwacji **wskazanego z góry auta**; klient przechodzi kreator (`/book/[token]`), wybiera termin na kalendarzu reagującym na realną dostępność w bazie, wpisuje pełne dane pod umowę (imię/nazwisko, e-mail, telefon, adres, nr dokumentu tożsamości, nr prawa jazdy) i kończy ekranem „poczekaj na potwierdzenie". W adminie nowa zakładka **Wnioski** (`/requests`) pokazuje zgłoszenia z akcjami **Potwierdź / Poproś o zmianę danych / Odrzuć**; przy każdej klient dostaje e-mail (Resend). „Potwierdź" tworzy klienta (dedup po e-mailu) + rezerwację; „Poproś o zmianę" wystawia nowy link z prefill i wysyła go mailem; „Odrzuć" powiadamia. Generowanie linku (wybór auta + opcjonalne sugerowane daty/stawka/kaucja/notatka) z „Kopiuj link" do wysłania klientowi (WhatsApp/SMS/mail).
- **Bezpieczeństwo publicznego endpointu.** Publiczny dostęp (niezalogowany klient) idzie **wyłącznie serwerowo** przez `service_role` scoped po nieodgadywalnym tokenie (`src/lib/book-public.ts`, bez `requireSession()`, wołane tylko z `/api/book/[token]`). Nowa tabela `rental.booking_links` ma RLS + `revoke all from anon` (wzór z lockdownu RLS) — anon nie ma żadnego dostępu. Publiczny GET zwraca **absolutne minimum**: nazwę/rejestrację/kolor tego jednego auta + jego zajęte zakresy dat (same daty) — zero danych innych klientów, aut czy cen. Serwerowa re-walidacja dostępności przy submit; wygaśnięcie 60 min egzekwowane przy odczycie (bez crona); token jednorazowy. `proxy.ts` przepuszcza `/book/*` i `/api/book/*` bez sesji i omija Basic Auth (klient nie zna hasła zespołu); reszta tras nadal redirectuje na `/login`. Zweryfikowane end-to-end na buildzie produkcyjnym: strona ładuje się (200), API oddaje tylko auto+zakresy, submit zapisuje wiersz `submitted` z pełnymi danymi, RLS/anon zablokowane.
- **Build przełączony na webpack (`next build --webpack`).** Turbopack lokalnie miał wadę — nie kompilował aktualnego `proxy.ts` (publiczne `/book/*` było gubione i redirectowane na `/login`), mimo poprawnego źródła (potwierdzone: `isPublicPath('/book/…')` = `true`; webpack build kompiluje proxy bez zarzutu). Żeby wdrożony proxy (bramka bezpieczeństwa) był gwarantowanie poprawny, build używa teraz webpacka. Uwaga dev: `npm run dev` (Turbopack) może lokalnie redirectować `/book/*` — do testu lokalnego użyj `npm run dev -- --webpack` albo `npm run build && npm run start`.
- **Handoff — e-mail Resend.** `src/lib/email.ts` wysyła przez Resend HTTP API i **degraduje się bez klucza** (log + pominięcie), więc kolejka akceptacji działa już teraz, tylko bez auto-maili. Do włączenia maili: konto Resend → klucz `RESEND_API_KEY` (+ `EMAIL_FROM`) w `.env.local`, w Vercel prod env i w vaulcie Infisical, oraz weryfikacja domeny (np. `mountaincar.is`) dla dostarczalności.

## 2026-07-04
- **Data integrity fix (RentHelp backfill).** A deterministic CSV↔database reconciliation (multiset diff by `type | dates`, tolerant of the manual fleet split, restored Polish diacritics, and OCR look-alikes) found that the original migration — done visually in two passes — had silently dropped **30 of 82** RentHelp calendar rows. The source rows were present and valid in the export CSV (written 9h before `seed.sql`); they were simply left out by the by-eye curation, not a parse error and not added later. Most alarming: **5 of the missing entries were future/active blocks** ([klient 195] 07-16, [klient 528]'s second Duster 08-16→30, [klient 908] 08-22, [klient 334] Witek 09-18, [klient 176] 09-19) — those vans showed as *free* in the app for dates RentHelp had blocked, a live double-booking risk. Backfilled all 32 missing entries (5 future blocks + 5 historical blocks + 22 historical reservations) plus 2 corrections ([klient 243] BPS82 end-date 08-25→08-30; the 2024 Pajero service moved from the Silver unit to the Blue unit per RentHelp) and removed a leftover test booking. Applied to production via the Management API and recorded idempotently (external_ref-guarded inserts, id-scoped updates/deletes) in `supabase/migrations/20260704120000_backfill_renthelp_missing.sql`, coherent with the seed→migrations replay (every backfilled reservation's customer already exists in `seed.sql`). Verified end-to-end: **82/82 CSV rows now reconcile, 0 missing**; the only DB surplus is the intentional split of the 2026 Pajero service across both physical Pajeros. Full method + tables: `docs/renthelp-reconciliation-2026-07-04.md`.
- **PWA install button on the login screen.** The "Zainstaluj aplikację" button now also appears on `/login`, not just inside the authenticated app shell — which is where it belongs for Android/desktop install, since an unauthenticated visitor lands on the login page first. Full-width secondary button below the sign-in card, shown only when the browser reports the app is installable (`beforeinstallprompt`); the service worker registers on `/login` too (via the always-mounted `AppShell`), so the install criteria are met there. Also dropped the `orientation: "portrait"` lock from `manifest.ts` so the installed window isn't forced portrait on a desktop.
- **Install button forces the native installer.** The "Zainstaluj aplikację" entry (left sidebar / mobile menu / login) calls the browser's own install prompt on click — `beforeinstallprompt` captured, `prompt()` fired on tap — so it opens the real native install dialog on Android/desktop Chrome & Edge, not a how-to. It appears only where the browser has armed that prompt (that event is the only mechanism a web page has to trigger installation — there is no unconditional "install now" API, and iOS Safari exposes none at all), and hides once the app is installed. The install event is captured at module load (before the button mounts), so a prompt Chrome fires early isn't missed and the button reliably shows whenever install is genuinely available. (Note: Chrome does not offer PWA install in Incognito/private mode — that's a browser rule, so testing there will always show the app without the button; use a normal tab on a device where it isn't already installed.)

## 2026-07-02
- Login: 365-day sessions, Sign in with Google (restricted to the one admin account).
- PWA: installable on Android/desktop (manifest, icons, service worker, install button).
- Bookings: working "Nowa rezerwacja" button on the list page.
- New Dashboard page: today's pickups/returns, bookings needing confirmation, next 7 days, month stats.
- Fleet: per-vehicle monthly utilization rings.
- Customers: company/individual profile split, notes, ID documents, suspect flag, edit/delete, reservation filter link.
- Contracts: company-vs-individual template block, identity-document lookup from real records, missing-data warning before sending, deep-linkable from a booking's "Podgląd umowy".
- Mobile optimization pass: touch targets, input types/autocomplete across forms.
- Code health pass: fixed a price-input bug (decimal entry was silently stripped), an open-redirect in the post-login redirect, two silent-failure UI bugs (contract send / document upload showing success when the save failed), and a stale-data race when switching customers on the contracts page.
- Mobile: fixed landscape phones incorrectly getting the desktop layout, condensed the mobile calendar further to fit more on one screen.
- Editing: bookings and vehicles can now be edited from the calendar, the bookings list, and the fleet list (not just created once).
- Sorting: bookings, fleet, and customers tables all support click-to-sort columns.
- Visual polish: toast notifications (replacing inline error banners), a top loading bar during initial data fetch, native View Transitions between pages — all respecting `prefers-reduced-motion`.
- Applied `20260702100554_customer_profile.sql` to production (company fields, `is_suspect`, `customer_documents` table + RLS) via the Supabase Management API.
- Bookings: added a month filter (prev/next nav, defaults to all months), combinable with the existing customer filter. Fixed a stale-closure bug in the shared `useSort` hook found while testing it (rapid clicks could drop a sort-direction toggle).
- **Security fix:** an architecture audit found that `customers`, `bookings`, `vehicles`, `contracts`, and `settings` had RLS policies open to the public `anon` role — anyone with the (publicly-shipped) anon key could read and write all of it with no login. Fixed by moving all Supabase access server-side (new `src/lib/actions.ts` + `src/lib/supabase-admin.ts`, `db.ts` now `server-only` and gated by a real session check on every call), then locking RLS back to `authenticated`-only. `proxy.ts` also now redirects unauthenticated visits to any page to `/login` (previously nothing enforced this). Verified end-to-end against the live database before and after. Vercel prod env vars (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) added and deployed; dead `NEXT_PUBLIC_SUPABASE_*` vars removed; confirmed `APP_AUTH_SECRET` was already set in prod. Full writeup: `docs/architecture-audit-2026-07-02.md`.
- **Reliability fix:** a quality-gate pass on the security refactor found that failed writes (a booking/customer/vehicle save or delete hitting a real DB error, e.g. a row deleted by someone else moments earlier) were either silently reported as fake successes or left the optimistic UI update in place with no rollback — meaning the screen could show a save that never actually happened. All mutation paths in `DataProvider.tsx` now roll back the optimistic change and show an error toast on failure, and the wizard/modals only proceed to their "done" state when the save genuinely succeeded. Verified against a real deleted-out-from-under-you row: correctly rolled back with no ghost data after reload.
